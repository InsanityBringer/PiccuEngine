SET (NETWORKING_SOURCES
		networking/networking.cpp)

IF(WIN32)
SET (NETWORKING_SOURCES
	${NETWORKING_SOURCES}
	networking/directplay.cpp)
ENDIF()

SET (NETWORKING_SOURCES ${NETWORKING_SOURCES} PARENT_SCOPE)