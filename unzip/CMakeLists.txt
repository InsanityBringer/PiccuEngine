SET (UNZIP_SOURCES
		unzip/infblock.h
		unzip/infcodes.h
		unzip/inffast.h
		unzip/inffixed.h
		unzip/inftrees.h
		unzip/infutil.h
		unzip/zconf.h
		unzip/zlib.h
		unzip/zutil.h 
		unzip/unzip.cpp
		unzip/adler32.c
		unzip/infblock.c
		unzip/infcodes.c
		unzip/inffast.c
		unzip/inflate.c
		unzip/inftrees.c
		unzip/infutil.c
		PARENT_SCOPE)
